Learning from this Project

```
  Custom error Classes
  Check the Mongo DB model, or user Model
  toJSON in mongoose model
  
```

Route Informations
```
  The currentUser route only to check if the current user is already looged in or not using the req.session.jwt is stored in the session or not.
```